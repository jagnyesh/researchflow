fastapi
uvicorn[standard]==0.22.0
httpx
sqlalchemy[asyncio]
asyncpg==0.28.0
aiosqlite>=0.20  # Required by langgraph-checkpoint-sqlite
pydantic
python-jose==3.3.0
pytest
pytest-asyncio
alembic
anthropic
docstring-parser>=0.15,<1.0  # Required by anthropic>=0.72.0
streamlit
extra-streamlit-components==0.1.71  # For cookie management
nest_asyncio==1.6.0
python-dotenv==1.0.0

# Multi-provider LLM support
aisuite>=0.1.6  # Multi-provider LLM client (version auto-resolved by UV)
openai>=1.0.0  # Optional: For OpenAI provider
# ollama  # Optional: For local Ollama models (installed separately)

# SQL-on-FHIR v2 dependencies
fhirpathpy==2.1.0
tenacity==8.2.3

# Research Notebook dependencies
pandas
plotly==5.14.1
scipy

# LangChain/LangGraph exploration dependencies
langchain>=0.3.7
langchain-core>=0.3.17  # Required by langchain-anthropic
langchain-community>=0.3.5
langchain-anthropic>=0.3.0
langgraph>=0.2.45
langgraph-checkpoint-sqlite>=3.0.0  # Required for LangGraph persistence
langsmith>=0.1.142  # For tracing and debugging

# Lambda Speed Layer dependencies (Sprint 5.5)
redis[hiredis]>=5.0.0  # Async Redis client with hiredis parser for performance
